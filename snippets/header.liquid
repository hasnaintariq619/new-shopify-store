<header
  class="relative flex justify-center items-center min-h-screen h-full w-full bg-cover bg-center pt-16"
  style="background-image: url('{{ 'header-bg.png' | asset_url }}');"
>
  <div class="container mx-auto px-4">
    <div class="grid grid-cols-1 lg:grid-cols-2 gap-8 items-center">
      <!-- Left Content -->
      <div>
        <div class="text-xl text-center md:text-left">
          <div class="font-extrabold uppercase">
            <span class="outline-text outline-white text-[2rem] md:text-[3.5rem] lg:text-[4.5rem]">get an</span>
            <br>
            <span class="text-white text-[1.8rem] md:text-[3rem] lg:text-[4rem]">intervention</span>
          </div>
        </div>
        <p class="text-white mt-4 text-sm md:text-md">
          Showing greater respect to older adults can be done in several ways: acknowledging and putting to use the
          talents and skills of older adults; creating intergenerational initiatives; and allowing older adults to stay
          active and independent for as long as possible.
        </p>
      </div>

      <!-- Right Content - Contact Form -->
      <div>
        <form
          action=""
          id="contact-form"
          class="form-container mx-auto max-w-lg p-8 md:py-16 rounded-lg bg-gradient-to-b from-[#2a7d6c] to-[#3e69609e] shadow-lg text-center"
        >
          <h1 class="text-[1.3rem] md:text-[2rem] text-white font-semibold mb-4">Contact Us</h1>

          <div class="grid grid-cols-1 md:grid-cols-2 gap-4">
            {% render 'custom-text-field',
              type: 'text',
              name: 'first-name',
              label: 'First Name',
              value: form.first_name,
              placeholder: 'First name',
              error: form.errors.first_name
            %}
            {% render 'custom-text-field',
              type: 'text',
              name: 'last-name',
              label: 'Last Name',
              value: form.last_name,
              placeholder: 'Last name',
              error: form.errors.last_name
            %}
          </div>

          <div class="grid grid-cols-1 md:grid-cols-2 gap-4 mt-4">
            {% render 'custom-text-field',
              type: 'text',
              name: 'business-email',
              label: 'Business Email',
              value: form.business_email,
              placeholder: 'Business Email',
              error: form.errors.business_email
            %}
            {% render 'custom-text-field',
              type: 'text',
              name: 'company',
              label: 'Company',
              value: form.company,
              placeholder: 'Company',
              error: form.errors.company
            %}
          </div>
          <div class="grid grid-cols-1 mt-4">
            {% render 'custom-dropdown',
              label: 'Country',
              options: 'Country 1|Country 2|Country 3',
              error: form.errors.country
            %}
          </div>

          {% render 'button', text: 'Submit', icon: 'send-icon.svg' %}
        </form>
      </div>
    </div>
  </div>

  <!-- Decorative Curves -->
  <div class="absolute -bottom-2 left-0 w-full z-10">
    <img src="{{ 'curve2.png' | asset_url }}" width="100%" height="auto" alt="" class="w-full h-24 object-cover">
  </div>
  <div class="absolute -bottom-2 left-0 w-full z-0">
    <img src="{{ 'curve1.png' | asset_url }}" width="100%" height="auto" alt="" class="w-full h-28 object-cover">
  </div>
</header>
