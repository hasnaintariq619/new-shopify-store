{
  "name": "layout",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "prepare:push": "concurrently  -c \"auto\" \"npm:dev:*\"",
    "dev:tailwind": "npx @tailwindcss/cli -i ./src/index.css -o ./assets/style.css --watch",
    "dev:shopify": "shopify theme dev --store=hxn-test-store"
  },
  "keywords": [],
  "author": "hxnain619",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "concurrently": "^9.1.2",
    "tailwindcss": "^4.0.3"
  }
}
